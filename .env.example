# Copy these from your dev.meta.ai dashboard (Model API section)
META_API_KEY=
META_BASE_URL=
# Model id, e.g. muse-spark-1.2  (standard tier)
# or muse-spark-1.2-contributor  (cheap tier — Meta may use prompts for training)
MODEL=muse-spark-1.2
